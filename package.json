{
  "name": "underwritly-landing",
  "private": true,
  "description": "Underwritly marketing landing page",
  "dependencies": {
    "@vercel/kv": "^3.0.0",
    "better-sqlite3": "^12.8.0",
    "nodemailer": "^8.0.3"
  },
  "scripts": {
    "start": "node dev-server.js",
    "dev": "node dev-server.js",
    "start:static": "npx --yes serve -l 3456 .",
    "deploy": "vercel --prod",
    "vercel:env": "node scripts/generate-vercel-env.js"
  },
  "devDependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
