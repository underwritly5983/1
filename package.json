{
  "name": "underwritly-landing",
  "private": true,
  "description": "Underwritly marketing landing page",
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "better-sqlite3": "^12.8.0",
    "nodemailer": "^8.0.3"
  },
  "scripts": {
    "start": "node dev-server.js",
    "dev": "node dev-server.js",
    "start:static": "npx --yes serve -l 3456 .",
    "deploy": "node scripts/deploy-vercel-all.js",
    "deploy:landing": "vercel deploy --prod --yes",
    "deploy:ifta": "node scripts/deploy-vercel-ifta.js",
    "db:verify": "node scripts/verify-database.js",
    "vercel:env": "node scripts/generate-vercel-env.js"
  },
  "devDependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
